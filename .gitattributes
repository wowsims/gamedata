# Collapsed in pull requests: the full item list moves by every item an update adds, and the
# patch folder's items.txt already shows those items on their own. spells.diff repeats the
# diff of spells/ for the record.
items/items.txt linguist-generated=true
patches/**/spells.diff linguist-generated=true
